pipeline {
   agent { label 'aws-ec2' }
    stages {
        stage('Build') {
            steps {
                sh 'df -h'
            }
        },
         stage('Build') {
            steps {
                sh 'du -sh'
            }
        },
         stage('Build') {
            steps {
                sh 'whoami'
            }
        }

    }
}

