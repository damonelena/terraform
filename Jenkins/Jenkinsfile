pipeline {
    agent { label 'aws-ec2' }
    stages {
        stage('Build') {
            steps {
                sh 'df -h'
            }
        }
        stage('Test') {
            steps {
                sh 'du -sh'
            }
        }
        stage('Deploy') {
            steps {
                sh 'whoami'
            }
        }
        stage('Deployed') {
            steps {
                sh 'hostname -I'
            }
        }
    }
}
